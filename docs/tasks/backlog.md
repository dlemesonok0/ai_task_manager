# Backlog

Этот файл хранит продуктовые и инженерные задачи, которые пока не готовы к немедленной реализации или имеют более низкий приоритет, чем задачи из `docs/tasks/current.md`.

## P1

### Нормальный вывод ошибок при регистрации

Цель: вместо заглушек показывать пользователю понятные сообщения об ошибках из API.

Объем работ:
- **Frontend**: в `handleLogin` парсить тело ответа при `!res.ok`, извлекать `detail` и показывать его в `authError`. Сейчас ошибка всегда `'Could not create account'`.
- **Backend**: проверить, что все `HTTPException` в `register` и `login` возвращают осмысленный `detail` (уже есть — `"Username must be at least 3 characters"`, `"Password must be at least 8 characters"`, `"Username is already registered"`).

Основные файлы:
- `frontend/src/App.tsx` (handleLogin, ~строка 200)
- `backend/main.py` (register/login endpoints)
- `backend/services/auth_service.py` (register_user)

Критерии приемки:
- При попытке зарегистрироваться с существующим username показывается `"Username is already registered"`.
- При коротком пароле показывается `"Password must be at least 8 characters"`.
- При коротком username показывается `"Username must be at least 3 characters"`.
- Ошибки логина тоже показывают detail из API.

### AI-парсинг задач

Цель: превращать естественный язык в структурированные задачи.

Критерии приемки:
- Backend принимает свободный текст и возвращает название, срок, длительность и приоритет.
- Frontend позволяет создать задачу из результата парсинга.
- Неоднозначные даты или отсутствующая длительность обрабатываются явно.

### AI-декомпозиция задач

Цель: разбивать крупные задачи на конкретные подзадачи.

Критерии приемки:
- Backend возвращает список подзадач с примерной длительностью.
- Пользователь может создать выбранные подзадачи из UI.
- Исходная задача не перезаписывается без подтверждения.

### Ежедневный ритуал планирования

Цель: провести пользователя через планирование и закрытие дня.

Критерии приемки:
- Утренний экран предлагает реалистичный план.
- Вечерний экран показывает выполненные, пропущенные и перенесенные задачи.
- Незавершенные задачи можно перенести вперед.

### Dashboard состояния синхронизации

Цель: сделать проблемы интеграций видимыми и понятными.

Критерии приемки:
- UI показывает время последней синхронизации по каждой интеграции.
- UI показывает последнюю ошибку синхронизации, если она есть.
- Пользователь может вручную запустить обновление или переподключение.

### Google OAuth Flow

Цель: заменить ручную настройку Google token JSON.

Критерии приемки:
- Пользователь может подключить Google Calendar из UI.
- Backend безопасно хранит токены.
- Истекшие токены можно обновить или переподключить.

### UI-улучшения

Цель: привести интерфейс к аккуратному, согласованному виду.

Объем работ:
- Выровнять счётчики по задачам и ивентам (Hero metrics, count-pill).
- Задизайнить кнопки выхода (Sign out) и обновления (Refresh) в шапке.
- Настроить анимацию загрузки карточек (skeleton loading).

Критерии приемки:
- Счётчики визуально согласованы (одинаковый отступ, шрифт, цветовая схема).
- Кнопки выхода и рефреша имеют иконки, hover-эффекты и соответствуют общему стилю.
- Skeleton-панели анимированы (пульсация или shimmer).

### UX-улучшения

Цель: убрать ручные шаги при подключении интеграций.

Объем работ:
- Автоматическая интеграция с Todoist через их webhook (вместо ручного ввода API-токена).
- Автоматическая интеграция с Google Calendar через OAuth flow в UI (вместо вставки token.json).
- Линк Telegram через кнопку в UI (без ручного копирования команды).

Критерии приемки:
- Пользователь может подключить Todoist, нажав кнопку и пройдя OAuth.
- Пользователь может подключить Google Calendar, нажав кнопку и пройдя OAuth.
- Пользователь может привязать Telegram одной кнопкой в интерфейсе.

### Проверка работоспособности Gemini API

Цель: удостовериться, что ключ Gemini API действительно работает, а AI-сервис отвечает.

Объем работ:
- Добавить endpoint `/api/health/ai` или расширить существующий health-check.
- Backend пробует выполнить минимальный запрос к Gemini (через `ai_service.client`).
- Если ключ невалиден или сервис недоступен — возвращать `status: "degraded"` с описанием проблемы.

Критерии приемки:
- Health endpoint показывает статус AI-провайдера.
- Ошибки (неверный ключ, таймаут, квота) логируются и возвращаются в ответе.
- Если AI-сервис не сконфигурирован — ответ без ошибки, `status: "disabled"`.

### Воркфлоу проекта

Цель: довести CI/CD, защиту веток и процесс разработки до продакшен-уровня.

Объем работ:
- Настроить branch protection rules для `master` (требовать PR, approved review, прохождение CI).
- Добавить lint-stage (pre-commit или через CI) — ruff для Python, eslint для фронта.
- Добавить typecheck (mypy или pyright) в backend CI.
- Добавить stage сборки (`npm run build`) в frontend CI.
- Разобраться с reject-direct-push — сейчас он падает на merge-коммитах с 3+ родителями.
- Добавить workflow для автоматического создания релизных тегов.
- Добавить docker-compose.prod.yml или вынести production-конфиг.

Критерии приемки:
- Прямой пуш в master невозможен на уровне GitHub (не только CI).
- Все проверки (lint, typecheck, тесты, сборка) проходят в PR.
- Deploy происходит только после мержа прошедшего все проверки PR.
- Процесс описан в CONTRIBUTING.md или AGENTS.md.

### Ускорение воркфлоу проекта

Цель: сократить время цикла разработки — от коммита до прогона тестов и деплоя.

Объем работ:
- Добавить кеширование зависимостей (uv cache, npm cache) в CI.
- Распараллелить backend и frontend тесты (сейчас идут последовательно).
- Ускорить сборку Docker-образов через BuildKit cache (gha cache уже есть, проверить).
- Заменить `pip install` в Dockerfile на `uv sync --no-dev` (быстрее и детерминированнее).
- Добавить `--changed-only` или фильтрацию тестов по затронутым файлам.
- Настроить GitHub Actions на запуск только релевантных джоб (backend/frontend) по изменённым путям.

Критерии приемки:
- Полный пайплайн (lint, typecheck, тесты, сборка) выполняется быстрее X минут (замерить до/после).
- Кеш зависимостей восстанавливается между запусками.
- Ни одна проверка не выпала — coverage и качество не пострадали.

## P2

### Модель workspace

Цель: подготовить архитектуру к командному использованию.

Критерии приемки:
- Пользователь принадлежит как минимум к одному workspace.
- Существующие персональные данные связаны с workspace по умолчанию.
- API-границы готовы к будущим проверкам ролей.

### Заготовки для billing

Цель: подготовить feature gating для SaaS-тарифов.

Критерии приемки:
- Есть тарифы free, pro и team.
- Проверки возможностей могут ограничивать premium-функции.
- Реальный платежный провайдер пока не требуется.

### Аналитика

Цель: показывать пользователю ценность продукта и прогресс.

Критерии приемки:
- Отслеживаются запланированные и выполненные задачи.
- Отслеживаются focus time и нагрузка встречами.
- Отслеживаются принятые и отклоненные предложения auto-schedule.

### Экспорт данных и удаление аккаунта

Цель: добавить контроль пользователя над своими данными.

Критерии приемки:
- Пользователь может выгрузить свои данные в JSON.
- Пользователь может удалить аккаунт и связанные данные интеграций.
- Поведение удаления покрыто тестами.
